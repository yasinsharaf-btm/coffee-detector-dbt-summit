version: 2

models:
  - name: metricflow_time_spine
    description: "Day-grain time spine required by the dbt Semantic Layer."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
