name: 'coffee_detector'
version: '1.0.0'
config-version: 2

profile: 'coffee_detector'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  coffee_detector:
    staging:
      +materialized: view
    marts:
      +materialized: table
